{
  "description": "Talos plugin hooks — detect talos context on session start",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/detect-talos-context.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
